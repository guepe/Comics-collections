No external Python packages are required.

This module depends only on standard Odoo 19 modules: ``base`` and ``mail``.
Install it from the Odoo Apps menu or by placing the module directory in your
``extra-addons`` path and running ``odoo-bin -u comics_collections``.
