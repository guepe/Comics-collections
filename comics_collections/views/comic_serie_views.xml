<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_comic_serie_list" model="ir.ui.view">
        <field name="name">comic.serie.list</field>
        <field name="model">comic.serie</field>
        <field name="arch" type="xml">
            <list string="Séries">
                <field name="name"/>
                <field name="type"/>
                <field name="statut"/>
                <field name="genre_id"/>
                <field name="editeur_id"/>
                <field name="nb_works_total" string="Tomes"/>
                <field name="a_suivre" string="Suivie" widget="boolean_toggle"/>
            </list>
        </field>
    </record>

    <record id="view_comic_serie_form" model="ir.ui.view">
        <field name="name">comic.serie.form</field>
        <field name="model">comic.serie</field>
        <field name="arch" type="xml">
            <form string="Série">
                <sheet>
                    <div name="button_box" class="oe_button_box"/>
                    <field name="image_couverture" widget="image"
                           class="oe_avatar" options="{'preview_image': 'image_couverture'}"/>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Titre de la série"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="type"/>
                            <field name="statut"/>
                            <field name="genre_id"/>
                            <field name="editeur_id"/>
                        </group>
                        <group>
                            <field name="nb_works_total"/>
                            <field name="a_suivre"/>
                            <field name="bdgest_id"/>
                            <field name="bedetheque_url" widget="url"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Synopsis">
                            <field name="synopsis"/>
                        </page>
                        <page string="Œuvres">
                            <field name="work_ids">
                                <list>
                                    <field name="tome"/>
                                    <field name="titre_canonique"/>
                                    <field name="edition_ids" widget="many2many_tags" readonly="1"
                                           string="Éditions" optional="hide"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_comic_serie_search" model="ir.ui.view">
        <field name="name">comic.serie.search</field>
        <field name="model">comic.serie</field>
        <field name="arch" type="xml">
            <search string="Séries">
                <field name="name" string="Titre"/>
                <field name="genre_id"/>
                <field name="editeur_id"/>
                <filter string="En cours" name="en_cours"
                        domain="[('statut', '=', 'en_cours')]"/>
                <filter string="Terminées" name="terminee"
                        domain="[('statut', '=', 'terminee')]"/>
                <filter string="À suivre" name="a_suivre"
                        domain="[('a_suivre', '=', True)]"/>
                <separator/>
                <filter string="BD" name="bd" domain="[('type', '=', 'bd')]"/>
                <filter string="Manga" name="manga" domain="[('type', '=', 'manga')]"/>
                <filter string="Comics" name="comics" domain="[('type', '=', 'comics')]"/>
                <group name="group_by">
                    <filter string="Type" name="group_type" context="{'group_by': 'type'}"/>
                    <filter string="Statut" name="group_statut" context="{'group_by': 'statut'}"/>
                    <filter string="Genre" name="group_genre" context="{'group_by': 'genre_id'}"/>
                    <filter string="Éditeur" name="group_editeur" context="{'group_by': 'editeur_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- ── Vue Kanban Séries (US-009) ──────────────────────────────────────── -->
    <record id="view_comic_serie_kanban" model="ir.ui.view">
        <field name="name">comic.serie.kanban</field>
        <field name="model">comic.serie</field>
        <field name="arch" type="xml">
            <kanban class="o_comic_kanban_series" sample="1">
                <field name="id"/>
                <field name="name"/>
                <field name="type"/>
                <field name="statut"/>
                <field name="has_cover"/>
                <field name="first_edition_cover_id"/>
                <field name="nb_works_total"/>
                <field name="editeur_id"/>
                <templates>
                    <t t-name="card">
                        <div class="o_comic_serie_card">
                            <!-- Couverture : propre > première édition > placeholder -->
                            <div class="o_comic_serie_cover">
                                <t t-if="record.has_cover.raw_value">
                                    <img loading="lazy"
                                         t-att-src="'/web/image/comic.serie/' + record.id.raw_value + '/image_couverture'"
                                         t-att-alt="record.name.value"/>
                                </t>
                                <t t-elif="record.first_edition_cover_id.raw_value">
                                    <img loading="lazy"
                                         t-att-src="'/web/image/comic.edition/' + record.first_edition_cover_id.raw_value + '/image_couverture'"
                                         t-att-alt="record.name.value"/>
                                </t>
                                <t t-else="">
                                    <div class="o_comic_serie_placeholder">
                                        <span t-esc="record.name.value"/>
                                    </div>
                                </t>
                                <span t-attf-class="o_comic_serie_statut o_statut_#{record.statut.raw_value}">
                                    <t t-esc="record.statut.value"/>
                                </span>
                            </div>
                            <!-- Infos -->
                            <div class="o_comic_serie_info">
                                <div class="o_comic_serie_title" t-esc="record.name.value"/>
                                <div class="o_comic_serie_meta">
                                    <t t-esc="record.editeur_id.value || ''"/>
                                </div>
                                <div class="o_comic_progress_label">
                                    <span><t t-esc="record.nb_works_total.raw_value"/> tomes</span>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="action_comic_serie" model="ir.actions.act_window">
        <field name="name">Séries</field>
        <field name="res_model">comic.serie</field>
        <field name="view_mode">kanban,list,form</field>
    </record>

</odoo>
